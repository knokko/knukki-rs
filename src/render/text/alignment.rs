#[derive(Copy, Clone, Eq, PartialEq, Debug)]
pub enum VerticalTextAlignment {
    Bottom,
    Center,
    Top,
}

#[derive(Copy, Clone, Eq, PartialEq, Debug)]
pub enum HorizontalTextAlignment {
    Left,
    Center,
    Right
}